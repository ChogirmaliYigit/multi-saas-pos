[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: touches the migration chain or other slow infrastructure
filterwarnings =
    ignore::DeprecationWarning
asyncio_default_fixture_loop_scope = function
